A WIP locally hosted manga server with a reader

Frontend built in preact and backend in go 

To run locally use two terminals 
start backend:
$ go run .\main.go

start frontend:
$ cd .\frontend\
$ npm run dev

